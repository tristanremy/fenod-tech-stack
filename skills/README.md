# Fenod skills

Short operating copy for agents. Law is `docs/stack-contract.md`. Depth is `examples/smoke`.

When a default in `docs/` changes, update the matching skill in the same commit. Keep each `SKILL.md` under ~150 lines.
