# Never commit real values. Prefer: infisical run --env=dev -- pnpm dev
# Local Workers secrets can also live in untracked .dev.vars

BETTER_AUTH_SECRET=replace-with-long-random-string
BETTER_AUTH_URL=http://localhost:3000
